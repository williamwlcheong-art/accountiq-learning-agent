---
phase: 05.1
slug: valuation-advisory-redesign
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-05-28
revised: 2026-05-30
---

# Phase 05.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

> **Revision note (2026-05-30):** Test file paths corrected to match the artifacts produced by Plan 02 and Plan 03 (`tests/test_research_loop.py`, `tests/test_report_viewer.py`). Wave numbers corrected to match plan frontmatter — Plan 02 is Wave 1 (independent of Plan 01, can run in parallel); Plan 03 is Wave 2; Plan 04 is Wave 3. Added entry for the new wizard-scoped endpoint test file from Plan 03 Task 4.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest 9.0.3 + pytest-asyncio 1.3.0 |
| **Config file** | none (uses default pytest discovery) |
| **Quick run command** | `source venv/bin/activate && python -m pytest tests/ -x -q 2>&1 \| tail -5` |
| **Full suite command** | `source venv/bin/activate && python -m pytest tests/ -v 2>&1` |
| **Estimated runtime** | ~10 seconds |

---

## Sampling Rate

- **After every task commit:** Run `source venv/bin/activate && python -m pytest tests/ -x -q 2>&1 | tail -5`
- **After every plan wave:** Run `source venv/bin/activate && python -m pytest tests/ -v 2>&1`
- **Before `/gsd-verify-work`:** Full suite must be green
- **Max feedback latency:** 15 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 05.1-01-01 | 01 | 1 | REPT-01 | — | Wave 0 RED tests for WACC scenarios, DCF correctness, decimal-form guard, normalised EBITDA, disclaimer compliance | unit | `source venv/bin/activate && python -m pytest tests/test_valuation.py -x -q` | ❌ W0 | ⬜ pending |
| 05.1-01-02 | 01 | 1 | REPT-01 | T-05.1-01, T-05.1-02 | valuation.py scoring removed; compute_wacc_scenarios returns H/M/L percent; compute_dcf + compute_illiquidity_discount preserved | unit | `source venv/bin/activate && python -m pytest tests/test_valuation.py::test_compute_wacc_scenarios_basic tests/test_valuation.py::test_compute_wacc_scenarios_returns_percent_not_decimal tests/test_valuation.py::test_dcf_correctness_fixed_inputs tests/test_valuation.py::test_normalised_ebitda_addback_sum tests/test_valuation.py::test_disclaimer_compliance_fmca -x -q` | ❌ W0 | ⬜ pending |
| 05.1-01-03 | 01 | 1 | REPT-01 | — | SECTION_SCHEMAS["valuation_advisory"] is the 12-key D-S1 list | unit | `source venv/bin/activate && python -c "import sys; sys.path.insert(0, 'backend'); from report_prompts import SECTION_SCHEMAS; assert len(SECTION_SCHEMAS['valuation_advisory'])==12"` | ❌ W0 | ⬜ pending |
| 05.1-02-01 | 02 | 1 | REPT-01 | T-05.1-05, T-05.1-06, T-05.1-07, T-05.1-08, T-05.1-10 | research_loop.py: WEB_SEARCH_TOOL config, ResearchBrief Pydantic, _apply_guardrails (4 guardrails), run_in_executor async wrapper | unit | `source venv/bin/activate && python -c "import sys; sys.path.insert(0, 'backend'); from research_loop import WEB_SEARCH_TOOL, ResearchBrief, run_valuation_research, _apply_guardrails; print('OK')"` | ❌ W0 | ⬜ pending |
| 05.1-02-02 | 02 | 1 | REPT-01 | T-05.1-05, T-05.1-08 | Offline tests: Pydantic field validation, 4 guardrails (decimal-form, placeholder, missing source, WACC range), JSON extraction, async-shape | unit | `source venv/bin/activate && python -m pytest tests/test_research_loop.py -x -q` | ❌ W0 | ⬜ pending |
| 05.1-03-01 | 03 | 2 | REPT-01 | T-05.1-14 | build_prompt() valuation_advisory emits TABLE_INSTRUCTION + research_brief + dcf_scenarios user message; other report types unchanged | unit | `source venv/bin/activate && python -m pytest tests/test_report_prompts.py -x -q` | ❌ W0 | ⬜ pending |
| 05.1-03-02 | 03 | 2 | REPT-01 | T-05.1-13 | _generate_report wires research → 3x DCF → disclaimer gate; _run_valuation_algorithm removed; main.py imports cleanly; max_tokens=8192 confirmed | unit | `source venv/bin/activate && python -c "import sys; sys.path.insert(0, 'backend'); from main import app, _generate_report" && source venv/bin/activate && python -m pytest tests/ -x -q --ignore=tests/test_research_loop.py` | ❌ W0 | ⬜ pending |
| 05.1-03-03 | 03 | 2 | REPT-01 | T-05.1-11, T-05.1-12 | wizard_report_view renders dict-valued sections; html.escape applied; XSS-safe; legacy string sections still render | unit | `source venv/bin/activate && python -m pytest tests/test_report_viewer.py -x -q` | ❌ W0 | ⬜ pending |
| 05.1-03-04 | 03 | 2 | REPT-01 | T-05.1-21, T-05.1-22 | New wizard-scoped endpoint GET /wizard/company/{id}/ebitda-adjustments uses Depends(get_current_user), enforces ownership-or-admin, returns 200 to wizard users | unit | `source venv/bin/activate && python -m pytest tests/test_wizard_endpoints.py -x -q` | ❌ W0 | ⬜ pending |
| 05.1-04-01 | 04 | 3 | REPT-01 | T-05.1-16, T-05.1-17, T-05.1-19, T-05.1-20 | Frontend intake form rebuilt + buildDynamicTable + normalisations collation + researching status; pre-fill calls /wizard/company/{id}/ebitda-adjustments; backend wizard endpoint registered | static + boot | `cd /Users/William.Cheong/accountiq_learning && grep -c "owner_key_person_dependency" frontend/index.html && grep -c "/wizard/company/" frontend/index.html && source venv/bin/activate && python -c "import sys; sys.path.insert(0, 'backend'); from main import app; routes=[r.path for r in app.routes if hasattr(r,'path')]; assert '/wizard/company/{company_id}/ebitda-adjustments' in routes"` | N/A (frontend HTML; gated by human-verify Task 2) | ⬜ pending |
| 05.1-04-02 | 04 | 3 | REPT-01 | — | End-to-end: non-admin user uploads → intake renders → pre-fill works (200, not 403) → research → 12-section report with tables + FMCA disclaimer + no truncation | manual (human-verify checkpoint) | — | N/A (authoritative quality gate for Plan 04) | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave Structure (cross-reference with plan frontmatter)

| Wave | Plans | Rationale |
|------|-------|-----------|
| 1 | 01, 02 | Both independent (`depends_on: []`); valuation refactor and research loop module are decoupled; can run in parallel |
| 2 | 03 | Depends on 01 (uses `compute_wacc_scenarios`) and 02 (uses `run_valuation_research`); cannot start until both Wave 1 plans land |
| 3 | 04 | Depends on 03 (needs the wizard-scoped endpoint from Plan 03 Task 4 + the table-rendering viewer extension) |

---

## Wave 0 Requirements

- [ ] `tests/test_valuation.py` — Wave 0 RED tests for REPT-01: WACC scenarios, decimal-form guard, DCF correctness (snapshot ±0.5%), normalised EBITDA, disclaimer compliance (Plan 01 Task 1)
- [ ] `tests/test_research_loop.py` — offline tests for Pydantic validation + 4 guardrails + JSON extraction + async-shape + WEB_SEARCH_TOOL config (Plan 02 Task 2)
- [ ] `tests/test_report_prompts.py` — 4 tests for valuation_advisory build_prompt() + TABLE_SECTIONS_VALUATION + other-report-type smoke (Plan 03 Task 1)
- [ ] `tests/test_report_viewer.py` — 5 tests for _render_report_sections_html dict-vs-string handling + html.escape XSS coverage (Plan 03 Task 3)
- [ ] `tests/test_wizard_endpoints.py` — 3 tests for new wizard-scoped /wizard/company/{id}/ebitda-adjustments endpoint: route registered, auth required, admin gate absent (Plan 03 Task 4)

*(Existing `tests/conftest.py` handles DB setup, ANTHROPIC_API_KEY loading, FastAPI test client — no new conftest work needed for the unit tests above)*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions | Plan / Task |
|----------|-------------|------------|-------------------|-------------|
| Normalisation table pre-fills from `/wizard/company/{id}/ebitda-adjustments` for a **non-admin** user (returns 200, not 403) | REPT-01 | Requires live DB + browser + non-admin auth session | Open wizard as non-admin → Valuation Advisory → confirm Network tab shows `/wizard/company/{id}/ebitda-adjustments` 200 and rows pre-fill | 04 / Task 2 (Step A) |
| 23-question form is absent | REPT-01 | Frontend DOM check | Open wizard → select Valuation Advisory → confirm no scoring questions visible | 04 / Task 2 (Step A) |
| All 12 report sections rendered in viewer with at least 3 HTML tables | REPT-01 | Requires full generation run with live Anthropic API | Generate a valuation report end-to-end; confirm 12 sections appear; confirm financial_performance, wacc_assumptions, valuation_summary render as `<table class='report-table'>` | 04 / Task 2 (Step D) |
| Disclaimer contains 4 FMCA phrases (`indicative`, `does not constitute financial advice`, `FMCA` or `Financial Markets Conduct`, `not relied` or `should not be relied`) | REPT-01 / REPT-06 | Requires generated report content | Open `/wizard/report/{id}/view`; read disclaimer section | 04 / Task 2 (Step D) |
| No output truncation at max_tokens=8192 boundary | REPT-01 | Requires full generation run | Scroll to end of viewer; confirm disclaimer fully written; if truncated, surface as blocker (contingency: two-pass strategy) | 04 / Task 2 (Step D) |
| WACC mid scenario is in 8–20% range; ≥1 source URL is rbnz.govt.nz or damodaran.com | REPT-01 | Requires real research output | Inspect rendered wacc_assumptions table and any source-URL display | 04 / Task 2 (Step E) |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies (Plan 04 Task 2 is explicitly checkpoint:human-verify — acceptable per task_completeness rules; Plan 04 Task 1 grep+boot is a static sanity check, human-verify Task 2 is the authoritative quality gate)
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify (verified — only Plan 04 Task 2 lacks automation; Tasks before and after have automation)
- [ ] Wave 0 covers all MISSING references (5 test files listed above)
- [ ] No watch-mode flags
- [ ] Feedback latency < 15s
- [ ] `nyquist_compliant: true` set in frontmatter (pending Wave 0 file creation)

**Approval:** pending
