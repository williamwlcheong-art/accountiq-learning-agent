---
phase: 05.1-valuation-advisory-redesign
plan: "02"
subsystem: api
tags: [anthropic, web-search, pydantic, research-loop, guardrails, agentic]

requires:
  - phase: 05.1-01
    provides: compute_wacc_scenarios and 12-section SECTION_SCHEMAS that consume ResearchBrief fields

provides:
  - backend/research_loop.py with WEB_SEARCH_TOOL, ResearchBrief, run_research_loop_sync, run_valuation_research, _apply_guardrails
  - 4-layer guardrail chain (decimal-form → placeholder → missing-source → WACC-range)
  - 20 offline unit tests covering all guardrails, Pydantic field validation, JSON extraction, async wrapper

affects: [05.1-03]

tech-stack:
  added: []
  patterns:
    - "Agentic research loop: Anthropic SDK web_search_20250305 tool with pause_turn resume cycle (max_iterations=5)"
    - "Pydantic v2 BaseModel as AI output contract — all fields validated before any business logic"
    - "Post-Pydantic guardrail function (_apply_guardrails) for domain-specific invariants"
    - "loop.run_in_executor wraps sync Anthropic SDK call inside async FastAPI context"

key-files:
  created:
    - backend/research_loop.py
    - tests/test_research_loop.py
  modified: []

key-decisions:
  - "adamodar URL matching: extend missing-source guardrail to include 'adamodar' — Damodaran's NYU URL path uses ~adamodar/ not 'damodaran'"
  - "No Pydantic retry-correction loop in v1 — surface ValidationError as RuntimeError on first failure; agent has max_uses=15 web search headroom"
  - "Phoenix tracing deferred — not added to v1 per plan scope"
  - "_apply_guardrails extracted as testable function (not inline in run_research_loop_sync) for offline test coverage"

patterns-established:
  - "Guardrail order: decimal-form WACC → placeholder text → missing RBNZ/Damodaran source → WACC range check"
  - "Offline test baseline: _valid_brief_kwargs() factory returns valid inputs; individual tests override one field to trigger failure"
  - "Duck-typed _StubResponse/_StubTextBlock for JSON extraction tests — no live Anthropic types imported in tests"

requirements-completed: [REPT-01]

duration: 9min
completed: 2026-05-30
---

# Plan 05.1-02: Agentic Research Loop Summary

**Anthropic web_search_20250305 research loop with ResearchBrief Pydantic contract and 4-layer guardrail chain — 20 offline tests all passing**

## Performance

- **Duration:** ~9 min
- **Completed:** 2026-05-30
- **Tasks:** 2/2
- **Files modified:** 2

## Accomplishments
- `backend/research_loop.py` (216 lines) — full agentic research loop using Anthropic SDK with server-side web_search tool, ResearchBrief Pydantic v2 model, and 4 guardrails
- `tests/test_research_loop.py` (20 offline tests) — covers Pydantic field validation (5), guardrails (11), JSON extraction (2), async shape (1), WEB_SEARCH_TOOL config (1)
- Fixed `adamodar` URL matching: Damodaran's NYU site uses `~adamodar/` path, not `damodaran`; guardrail now matches both

## Task Commits

1. **Task 1: Implement backend/research_loop.py** - `8b62e5b` (feat)
2. **Task 2: Offline guardrail tests + fix adamodar URL matching** - `f102a4f` (feat)

## Files Created/Modified
- `backend/research_loop.py` — WEB_SEARCH_TOOL constant, ResearchBrief Pydantic model, _apply_guardrails (4 checks), _extract_json_from_response, run_research_loop_sync (sync loop w/ max_iterations=5), run_valuation_research (async wrapper via run_in_executor)
- `tests/test_research_loop.py` — 20 offline tests, no live API calls, all pass in 0.01s

## Decisions Made
- Extended missing-source guard to match `'adamodar'` in addition to `'damodaran'` — real RBNZ/Damodaran URLs use the `~adamodar/` path fragment
- No Pydantic retry-correction loop in v1: ValueError surfaced immediately; max_uses=15 web search headroom allows the agent to self-correct in most cases
- `_apply_guardrails(brief)` extracted as a standalone function to enable direct offline testing without mocking the Anthropic SDK

## Deviations from Plan
- **adamodar URL fix:** Plan specified `"damodaran"` as the missing-source substring match. Real Damodaran NYU URLs use `~adamodar/` path — extended the guard to match both during Task 2 verification. No scope change; same guardrail, corrected substring.

## Issues Encountered
- Execution environment blocked Write/Bash tool calls for commit and SUMMARY creation after Task 1; orchestrator completed Task 2 commit and this SUMMARY directly.

## User Setup Required
- `ANTHROPIC_API_KEY` must be set in `.env` (already present — used by ingestion.py)
- Anthropic account tier must support `web_search_20250305` tool (confirm at platform.claude.com → Settings → Tool Use)

## Next Phase Readiness
- `run_valuation_research(company_name, company_location, industry_sector) -> ResearchBrief` is the stable import target for Plan 03
- All 4 guardrails active and tested; Plan 03 can call `run_valuation_research` and trust the returned brief passes domain invariants
- xfail test in test_valuation.py (decimal guard on ResearchBrief) will flip to pass once Plan 03 wires up the research loop

---
*Phase: 05.1-valuation-advisory-redesign*
*Completed: 2026-05-30*
