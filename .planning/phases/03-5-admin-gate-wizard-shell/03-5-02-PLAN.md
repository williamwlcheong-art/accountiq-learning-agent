---
phase: "03-5"
plan: "02"
type: execute
wave: 2
depends_on:
  - "03-5-01"
files_modified:
  - backend/main.py
autonomous: true
requirements:
  - AUTH-09

must_haves:
  truths:
    - "Non-admin users receive 403 on all existing /companies/*, /documents/*, /financials/*, /patterns/*, /analytics/*, /settings/* routes"
    - "Admin users receive the normal 200/201/204 on all those routes"
    - "Unauthenticated callers receive 401 (not 403) on all those routes"
    - "The /health and /auth/* routes are unaffected (no auth required)"
    - "The existing 37-test suite passes without regressions"
  artifacts:
    - path: "backend/main.py"
      provides: "All 25 existing routes swap Depends(get_current_user) for Depends(require_admin)"
      contains: "Depends(require_admin)"
  key_links:
    - from: "backend/main.py:every_admin_route"
      to: "backend/auth.py:require_admin"
      via: "Depends(require_admin) in each route signature"
      pattern: "Depends\\(require_admin\\)"
---

<objective>
Apply require_admin to every existing admin-facing route in main.py, turning non-admin callers away with 403 while leaving /health, /auth/*, and the (forthcoming) /wizard/* routes unaffected.

Purpose: AUTH-09 success criterion 3 — non-admin callers are rejected server-side by a backend dependency, not just by frontend JS. This is the authoritative gate.

Output: backend/main.py with all 25 existing route `Depends(get_current_user)` occurrences replaced by `Depends(require_admin)`. Tests previously RED now GREEN.
</objective>

<execution_context>
@/Users/William.Cheong/accountiq_learning/.claude/get-shit-done/workflows/execute-plan.md
@/Users/William.Cheong/accountiq_learning/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/William.Cheong/accountiq_learning/.planning/PROJECT.md
@/Users/William.Cheong/accountiq_learning/.planning/ROADMAP.md
@/Users/William.Cheong/accountiq_learning/.planning/phases/03-5-admin-gate-wizard-shell/03-5-CONTEXT.md
@/Users/William.Cheong/accountiq_learning/.planning/phases/03-5-admin-gate-wizard-shell/03-5-RESEARCH.md
@/Users/William.Cheong/accountiq_learning/.planning/phases/03-5-admin-gate-wizard-shell/03-5-PATTERNS.md
@/Users/William.Cheong/accountiq_learning/.planning/phases/03-5-admin-gate-wizard-shell/03-5-01-SUMMARY.md

<interfaces>
<!-- Exact route inventory in backend/main.py that needs the dependency swap. -->
<!-- Read main.py before making any changes. -->

Routes that MUST receive Depends(require_admin) (replace Depends(get_current_user)):
  Line  82: list_companies — GET /companies
  Line 110: create_company — POST /companies
  Line 130: get_company — GET /companies/{company_id}
  Line 152: update_company_profile — POST /companies/{company_id}/profile
  Line 184: profile_status — GET /companies/{company_id}/profile-status
  Line 265: list_management_team — GET /companies/{company_id}/management-team
  Line 288: add_management_team_member — POST /companies/{company_id}/management-team
  Line 313: update_management_team_member — PUT /companies/{company_id}/management-team/{member_id}
  Line 336: delete_management_team_member — DELETE /companies/{company_id}/management-team/{member_id}
  Line 358: list_ebitda_adjustments — GET /companies/{company_id}/ebitda-adjustments
  Line 381: add_ebitda_adjustment — POST /companies/{company_id}/ebitda-adjustments
  Line 406: update_ebitda_adjustment — PUT /companies/{company_id}/ebitda-adjustments/{adj_id}
  Line 429: delete_ebitda_adjustment — DELETE /companies/{company_id}/ebitda-adjustments/{adj_id}
  Line 453: list_documents — GET /documents
  Line 530: upload_document — POST /documents/upload
  Line 636: document_status — GET /documents/{document_id}/status
  Line 665: document_rows — GET /documents/{document_id}/rows
  Line 693: company_financials — GET /financials/{company_id}
  Line 726: list_patterns — GET /patterns
  Line 747: export_patterns — GET /patterns/export
  Line 769: analytics_overview — GET /analytics/overview
  Line 812: confidence_stats — GET /analytics/confidence
  Line 831: get_settings — GET /settings  (signature: `current_user: dict = Depends(get_current_user)`)
  Line 847: update_settings — POST /settings
  Line 876: retry_document — POST /documents/{document_id}/retry

Routes that MUST NOT receive require_admin (leave as-is):
  /health (line 70) — no auth dependency
  /auth/* — handled by auth_router
  GET / (line 916) — no auth dependency

Require_admin is now exported from auth.py (Plan 01 task). The import at line 25 already reads:
  `from auth import auth_router, get_current_user, require_admin`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap Depends(get_current_user) → Depends(require_admin) on all 25 admin routes</name>
  <files>backend/main.py</files>
  <read_first>
    - backend/main.py (full file) — read the entire file before editing to see current state; confirm the import line already includes `require_admin`; confirm the 25 occurrences listed in the interfaces block
    - backend/auth.py (lines 98-110) — confirm require_admin signature before referencing it
  </read_first>
  <action>
In `backend/main.py`, perform the dependency swap on all 25 existing admin-facing routes. The change on each route is a single-line replacement:

FROM: `    current_user: dict = Depends(get_current_user),`
TO:   `    current_user: dict = Depends(require_admin),`

Affected lines (from the interfaces block above): 82, 110, 130, 152, 184, 265, 288, 313, 336, 358, 381, 406, 429, 453, 530, 636, 665, 693, 726, 747, 769, 812, 831, 847, 876.

**Special case — get_settings (line 831):** This function uses a bare function-signature parameter with no comma after it:
```python
async def get_settings(current_user: dict = Depends(get_current_user)):
```
Change to:
```python
async def get_settings(current_user: dict = Depends(require_admin)):
```

**What NOT to change:**
- The import line `from auth import auth_router, get_current_user, require_admin` — leave as-is (get_current_user is still needed by require_admin internally and may be referenced elsewhere)
- `/health` route (line 70) — no auth dependency; do not add one
- `GET /` route (line 916) — no auth dependency; do not add one
- The new `/wizard/upload` route that Plan 03 will add — it uses `get_current_user` (not require_admin)
- `_resolve_or_create_company` and `_run_ingestion` helper functions — not routes

After the edit, verify the swap count:
```bash
grep -c "Depends(require_admin)" backend/main.py  # must be 25
grep -c "Depends(get_current_user)" backend/main.py  # must be 0 (all swapped)
```
The import line does not count toward the grep because it does not contain `Depends(`.
  </action>
  <verify>
    <automated>cd /Users/William.Cheong/accountiq_learning && source venv/bin/activate && pytest tests/test_admin_gate.py -x -q 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Depends(require_admin)" backend/main.py` outputs `25`
    - `grep -v "^from\|^import\|#" backend/main.py | grep -c "Depends(get_current_user)"` outputs `0` (no route-level usage of get_current_user remains)
    - `pytest tests/test_admin_gate.py::test_regular_user_companies_403 -x -q` passes GREEN
    - `pytest tests/test_admin_gate.py::test_regular_user_financials_403 -x -q` passes GREEN
    - `pytest tests/test_admin_gate.py::test_regular_user_patterns_403 -x -q` passes GREEN
    - `pytest tests/test_admin_gate.py::test_regular_user_settings_403 -x -q` passes GREEN
    - `pytest tests/test_admin_gate.py::test_admin_user_companies_200 -x -q` passes GREEN
    - `pytest tests/test_admin_gate.py::test_unauthenticated_returns_401_not_403 -x -q` passes GREEN
    - `pytest tests/ -q` shows >= 37 tests passing, no regressions
  </acceptance_criteria>
  <done>
    All 25 admin-facing routes use Depends(require_admin). Non-admin users receive 403. Admin users receive 200. Unauthenticated callers receive 401. Full test suite green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client → all existing admin API routes | Any authenticated user can call these routes; require_admin is the server-side gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-5-05 | Elevation of Privilege | All 25 admin routes in main.py | mitigate | `Depends(require_admin)` on each route; 403 for non-admin; 401 for unauthenticated. Verified by integration tests. |
| T-03-5-06 | Elevation of Privilege | Missed route (incomplete swap) | mitigate | Post-edit grep assertion: `grep -c "Depends(require_admin)" backend/main.py` must equal 25; CI fails if any route was missed |
| T-03-5-07 | Tampering | `/health` or `/auth/*` accidentally gated | accept | These routes have no `current_user` parameter; the grep for `Depends(get_current_user)` on non-import lines = 0 confirms no accidental gating |
</threat_model>

<verification>
```bash
cd /Users/William.Cheong/accountiq_learning && source venv/bin/activate
grep -c "Depends(require_admin)" backend/main.py          # must be 25
grep -v "^from\|^import\|#" backend/main.py | grep -c "Depends(get_current_user)"  # must be 0
pytest tests/test_admin_gate.py -q
pytest tests/ -q
```
</verification>

<success_criteria>
- `grep -c "Depends(require_admin)" backend/main.py` returns 25
- 6 previously-RED admin gate tests now GREEN
- 3 wizard xfail tests remain XFAIL
- Full test suite (pytest tests/ -q) shows >= 37 passing, 0 failed
</success_criteria>

<output>
After completion, create `.planning/phases/03-5-admin-gate-wizard-shell/03-5-02-SUMMARY.md`
</output>
